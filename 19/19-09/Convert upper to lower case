#include<iostream>
using namespace std;
int main()
{
	char ch;
	cout<<"Enter the character";
	cin>>ch;


	if(ch>=65 && ch<=90)   //if condition
	{
		ch=ch+32;        
		cout<<"the char is converted into lower case "<<ch;
}
	else{
		"invalid";
	}


}
